/*
import { PaginationInput } from "__generated__/types";

export const PAGE_SIZE = 10;

export const defaultPagination: Readonly<PaginationInput> = Object.freeze({
    limit: PAGE_SIZE,
    offset: 0,
});
 */
